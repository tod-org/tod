# Done

- **What was built**: Fixed `scripts/test.sh` so the CI gate can actually fail. Replaced the `if rg … then exit 1; fi` block (which severed the `&&` chain and forced exit status 0 via the trailing echo) with the `! rg … &&` form, keeping the chain intact so any failing step — including `cargo nextest run` — now makes the whole script exit non-zero.
- **Commit SHA(s)**: `64d4df6d0c6413cd44ba799f51ff2e1b07c2cd58` — `fix: make the gate in scripts/test.sh able to fail` (pushed to `origin/alanvardy-var-1039-tod-gate-cannot-fail-an-if-block-severs-the-chain-in`; PR #1803).
- **Verification**: `bash -n scripts/test.sh` passes; DoD demonstrated in `/tmp` — a file containing a TODO makes the `! rg … &&` chain exit non-zero and skip the trailing echoes, while a match-free file exits zero. Repo `rg` read-only scan finds no forbidden strings (happy path green). The two known-failing Rust tests (`test_process_with_filter`, `test_process_with_project`) are out of scope and were not touched; the full `./scripts/test.sh` cargo steps were not run because they would fail on those unrelated tests.
- **Reviewer findings**: No blockers. Two nits noted: (1) `!` collapses an `rg` error (non-0/1 exit) to success — inherent to the task-specified form and not a regression (the old `if` block behaved the same); (2) Conventional-Commits compliance could not be checked by the shell-less reviewer — verified by parent: commit message `fix: …` is CC-compliant.
- **Remaining manual items**: The two known-failing `tod` tests in `src/lists.rs` remain broken and are tracked separately (out of scope here). PR #1803 is still a draft — mark ready for review / merge (`gh pr merge 1803 --rebase --delete-branch`) when the two out-of-scope tests are handled. Pre-existing unrelated working-tree items (tracked deletion of `DELETEME`, untracked `.pi/orksorksorks/`) were left untouched; the full repo gate will report red on the two out-of-scope tests until they are fixed separately.