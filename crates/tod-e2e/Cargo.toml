[package]
name = "tod-e2e"
version = "0.1.3"
edition = "2024"
publish = false
description = "An external End2End Test Crate for the Tod Binary that tests it against live Todoist API/Developer Account"

[dev-dependencies]
assert_cmd = "2.2.2"
predicates = "3.1.4"
reqwest = { version = "0.13", features = ["blocking", "json"] }
serde_json = "1.0.150"
tempfile = "3.27.0"
